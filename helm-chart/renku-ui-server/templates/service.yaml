apiVersion: v1
kind: Service
metadata:
  name: {{ template "ui-server.fullname" . }}
  labels:
    app: {{ template "ui-server.name" . }}
    chart: {{ template "ui-server.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ template "ui-server.name" . }}
    release: {{ .Release.Name }}
